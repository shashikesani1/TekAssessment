# TekAssessment
Assessment to develop calculator in Angular2

Open the solution in Visual studio and Run. It should launch the application directly. Just in case if there are any issues, follow below steps.

Install below npm if the code does not work
---------------------------------------------------------------------------
npm install

npm install --save @types/core-js so that core-js 

npm install gulp-typescript typescript

npm install --save-dev gulp-clean

npm install --save-dev gulp

npm install jasmine-core --save-dev

npm install --save-dev jasmine

npm install --save-dev karma

karma init



